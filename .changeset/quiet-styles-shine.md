---
"openwiki": patch
---

The visualizer's styles now live in a standalone `styles.css` instead of being inlined into the page, so you can restyle the graph and reader without rebuilding. Static exports write it alongside `index.html`.
